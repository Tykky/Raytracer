language: cpp
os: linux
compiler: gcc

before_script:
        - mkdir build
        - cd build
        - cmake ..
script:
        - make
        - ./Test/gtest_run

after_success:
         - bash <(curl -s https://codecov.io/bash)
